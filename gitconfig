# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
	name = Mike Moate
	email = github@mikemoate.co.uk
[diff]
	tool = bc3
[merge]
	tool = bc3
[mergetool "bc3"]
	trustExitCode = true
[core]
	excludesfile = /Users/mikemoate/.gitignore_global
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
